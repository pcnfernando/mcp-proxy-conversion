# graphql-service - MCP Server

This project was auto-generated by the Ballerina MCP tool (`bal mcp`).
It provides a Ballerina MCP server that wraps the **graphql-service** API (vv1.0).

## Project Structure

```
├── Ballerina.toml    # Project configuration
├── main.bal          # MCP service with remote functions per API endpoint
├── types.bal         # Ballerina record types from API schemas
└── README.md         # This file
```

## Prerequisites

- [Ballerina Swan Lake](https://ballerina.io/downloads/)
- The upstream API must be reachable at: `https://choreoapis.dev/jan28/graphql-service/v1.0`

## Running the MCP Server

```bash
bal run
```

The MCP server will start on port **9090**.

## Available Tools

| Function | Method | Path | Description |
|---|---|---|---|
| `operation1PostDefault` | POST | `/*` | Executes POST on /* |


## Configuration

To change the upstream API base URL, update the `apiClient` declaration in `main.bal`:

```ballerina
http:Client apiClient = check new ("<your-api-base-url>");
```

---
*Generated by `bal mcp` — Ballerina OpenAPI-to-MCP Server Generation Tool*
